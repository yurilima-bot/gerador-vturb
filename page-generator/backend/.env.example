# Porta do servidor (Vercel define automaticamente em produção)
PORT=3001

# URLs permitidas no CORS (separadas por vírgula)
CORS_ORIGIN=http://localhost:3000,https://gerador-vturb-site.vercel.app

# Ambiente
NODE_ENV=development
